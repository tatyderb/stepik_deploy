# Текст в формате markdown

lesson: 1945910

---1234 TEXT

## Форматы текста.

Это одна строка текста.

Это блок из первой строки.
И второй строки. Их нужно сделать в одном параграфе.

Текст бывает *italic*, **bold** и ***bold italic***, потом идет обычный текст.

Вставка текста `monospace` и вставка $e = mc^2$ формулы LaTeX.

Это блок из первой строки. Перевод строки   
И второй строки. Их нужно сделать в одном параграфе, но после первой строки поставить br.

---1234 TEXT

## Списки

Уровни вложений отбиваем 4 пробелами. Табуляции не работают.

* Верхний уровень 1
  + вложение 2
    - вложение 3

Еще один список, теперь с табуляциями

* верхний уровень 2
  * вложение 2
  - вложение 3
* верхний уровень 3

---1234 TEXT

## Заголовки

Текст до заголовка 3.

### Заголовок уровня 3

#### Заголовок уровня 4

##### Заголовок уровня 5

###### Заголовок уровня 6

Дальше текст.

---1234 TEXT

## Таблицы

Две таблицы склеятся в одну. Напишите между ними текст, чтобы избежать склейки.

| Заголовок 1 | Заголовок такой же ширины |
| --- | --- |
| Термин 1 | Определение 1 |
| Термин 2 | Определение 2, которое будет длинным и содержать *italic*, **bold** и потом формулу |
| Термин 3 с пояснениями | Определение 3 |

| одна часть | три части |
| --- | --- |
| Термин 1 | Определение 1 |
| Термин 2 | Определение 2, которое будет длинным и содержать *italic*, **bold** и потом формулу |
| Термин 3 с пояснениями | Определение 3   Продолжение на другой строке |
| Термин 4   Термин 5 | Определение 4 и 5 |

Таблица в HTML

* stepik:
* `th` заменили на `td`
* `border = 1` сохранился
* `width="25%"` удалили, ширины делаются автоматически
* `colspan`, `rowspan` - работает

| Заголовок | Двойной столбец | Еще заголовок |
| --- | --- | --- |
| 25% | 50% | 25% |
| небольшие данные | большое объяснение с примерами и неразрывным пробелом | большое объяснение с примерами и неразрывным пробелом |
| `ls` | Переименование файла myprog.c в файл yourprog.c и просмотр   Перемещение файла из myWorkFolder в текущий каталог. В текущем каталоге появится файл с таким же именем, в каталоге myWorkFolder будет удален.  Перемещение файла otherfile в каталог myWorkFolder. В каталоге myWorkFolder появится файл с таким же именем, в текущем будет удален. | ``` ls >ls myprog.c >mv myprog.c yourprog.c >ls yourprog.c >cp myWorkFolder/myprog.c . >cp otherfile myWorkFolder/ ``` |

### Еще таблица с colspan и rowspan

|  |  |  |
| --- | --- | --- |
| rowspan 2 | a 12 | a 13 |
| a 22 | a 23 |
| a 31 | a 32 | |

---1234 TEXT

## Ссылки и рисунки

* [Stepik](http://stepik.org)

И внутри текста расположим ссылку на текст.

![Логотип Stepik](https://stepik.org/static/frontend/topbar_logo.svg)

---1234 TEXT

## Codeblock

Нужно уметь писать куски кода на языке С внутри строки, например `int * px = &x;` и все будет хорошо.

Не надо надеяться, что *px и &x будут правильно сформатированы.

Как и **argc, например.

Программа на python:

```python
print('Hello, world!')
```

Программа на С:

```cpp
#include <stdio.h>
int main(int argc, char * argv[]) {
printf("Hello, world!\n");
return 0;
}
```

Командная строка:

```bash
> pwd
/home/student
> dir
workspace
```

Просто блок без указания языка:

```
ааа
ббб
ссс
```

4 пробела или табуляция - НЕ РАБОТАЕТ!

```
#include <stdio.h>
int main(int argc, char * argv[]) {
printf("Hello, world!\n");
return 0;
}
```

Нумерация строк (не работает, пробовала разные варианты, снесла, оставив чистый cpp):

```cpp
#include <stdio.h>
int main(int argc, char * argv[]) {
printf("Hello, world!\n");
return 0;
}
```

---1234 TEXT

## LaTeX

Вставка отдельной формулы $e = mc^2$ в тексте. В него подставим отдельно стоящую формулу $$y = \sin{x}^2 $$ и напишем дальше текст.

---1234 TEXT

## Внутри вставок кода markdown с разметкой шагов

Это текст до вставки кода.

```
# Это не заголовок, это пример кода внутри шага (коментарий python)
## И это не заголовок. А код внутри текстового шага.
Просто код.
Вторая строка того же абзаца.
## Еще один не заголовок
Конец кода.
```

Это текст после вставки кода.

---1234 TEXT

Был без заголовка и без ключевого слова TEXT. Это первый абзац текста. Не должно быть пустого места, не должно быть тега h2 в тексте шага.

Это второй абзац текста.

---1234 TEXT

Тоже без заголовка, ключевое слово TEXT не должно быть напечатано.
